<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>ActEuropa</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/common/css/normalize.css">
    <link rel="stylesheet" href="/blog/css/main.css">
    <link rel="stylesheet" href="/common/css/metricsgraphics.css">
    <link rel="stylesheet" href="/common/css/nice-select.css">
    <% include ../../Partials/Favicons+Fonts.html %>
</head>

<body>
    <% include ../../Partials/Header.html %>
    <style>
        .blogItem{
            background:white;
            box-shadow: 0 1px 2px rgba(0,0,0,0.15);
            
            padding:12px;
            transition:all 200ms;
            margin-top:18px;
        }
        .cover{
            background-size:cover;
            background-position: center center;
            overflow:hidden;
            max-height:256px;
            height:256px;
        }
        .content{
            margin-top:24px;
            margin-bottom:24px;
        }
        .content a{opacity:1;}
        .content a:hover{opacity:1;}
        .content a:hover .blogItem{
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }
        .content a:active{opacity:1}
        .content a:active .blogItem{
            box-shadow: 0 5px 15px rgba(0,0,0,0.5);
        }

        .content h3{
            color:#002f55;
            font-family: Montserrat, sans-serif;
            font-size:2em;
            font-weight:400;
            margin-bottom:6px;
        }
        .content h4{
            font-weight:400;
            margin:0;
            opacity:0.75;
            line-height:1.5em;
        }
        .content h5{
            margin:0;
            margin-top:12px;
            color: #c73e12;
            font-weight:400;
            font-family: Montserrat, sans-serif;
        }
        .verified-icon{
            max-width:1em;
            max-height:1em;
            margin-right:4px;
        }
    </style>
    <div class="center content">
    <% for(var i=0; i<posts.length; i++) { %>
        <a style="text-decoration:none;" href='<%= posts[i].url %>'>
        <div class="blogItem">
            <div class="cover" style="background-image: url('<%= posts[i].heroimage %>');"></div>
            <h3><% if(posts[i][lang] == undefined) { %><%= posts[i].en.title %><% } else { %><%= posts[i][lang].title %><% } %></h3>
            <h4><% if(posts[i][lang] == undefined) { %><%= posts[i].en.subtitle %><% } else { %><%= posts[i][lang].subtitle %><% } %></h4>
            <h5><img class="verified-icon" src="/common/img/Primary_Logo.svg"></img><%= posts[i].author %> <span style="color:black;opacity:0.5;">• <%= posts[i].date.toDateString() %></span></h5>
        </div>
        </a>
    <% } %>
    </div>
    <% include ../../Partials/Footer.html %>
</body>

</html>